import React, { Component } from 'react';
import io from 'socket.io-client';

class Main extends Component {
  
  render(){
      return (
        <div>
            <h5>Welcome video</h5>
        </div>
      );
  }
}

export default Main;